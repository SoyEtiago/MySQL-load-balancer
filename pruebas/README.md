# Pruebas Sysbench (Docker / ProxySQL)\n\n```bash\nchmod +x pruebas/*.sh scripts/*.sh\n# preparar dataset\n./pruebas/00_prepare_dataset.sh\n# lanzar matriz principal\n./pruebas/run_matrix.sh\n# (opcional) monitorear lag\n./pruebas/09_replication_lag_monitor.sh 5\n# (opcional) simular caída de réplica\n./pruebas/10_fail_replica_during_ro.sh\n# limpieza\n./pruebas/13_cleanup_dataset.sh\n```\n\nConfigura variables en `scripts/common.sh` si cambias nombres/credenciales.\n
